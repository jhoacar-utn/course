# Liam Marin - Final Project

## Running

* Copy `.env.example` to `.env` and fill in the required variables. All variables are documented inside the [example file](.env.example).

* Build and run the project.
  ```bash
  npm run build && npm start
  ```
